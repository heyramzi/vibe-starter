# UI Components (shadcn/ui)

Primitive components from [shadcn/ui](https://ui.shadcn.com). Do not modify.

## Reinstall

```bash
# From project root
../../scripts/reinstall-shadcn.sh
```

This reinstalls all components with `--all --overwrite`, adding new ones and updating existing.
