# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env.local (Next.js) or .env (SvelteKit)
# NEVER commit .env.local or .env to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (New API Key System)
# -----------------------------------------------------------------------------
# Get these from: https://app.supabase.com → Your Project → Settings → API Keys
#
# Supabase now uses:
# - Publishable key (sb_publishable_...) - Safe for browser/client
# - Secret key (sb_secret_...) - Server-side only, bypasses RLS
#
# The old anon/service_role JWT keys still work but publishable/secret are preferred

# For Next.js (NEXT_PUBLIC_ prefix exposes to browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# For SvelteKit (PUBLIC_ prefix exposes to browser)
# PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
# PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Server-side only - bypasses Row Level Security (use with caution!)
SUPABASE_SECRET_KEY=sb_secret_...

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

# For Next.js
NEXT_PUBLIC_APP_URL=http://localhost:3000

# For SvelteKit
# PUBLIC_APP_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# Optional: Database URL (for migrations/backups)
# -----------------------------------------------------------------------------
# Get from: Supabase Dashboard → Settings → Database → Connection string
# SUPABASE_DB_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project-id.supabase.co:5432/postgres

# -----------------------------------------------------------------------------
# Optional: Additional Services
# -----------------------------------------------------------------------------
# Add your service-specific environment variables below

# AI Services
# OPENAI_API_KEY=sk-...
# GOOGLE_GEMINI_API_KEY=...

# Payment Processing
# STRIPE_SECRET_KEY=sk_...
# STRIPE_PUBLISHABLE_KEY=pk_...

# Email Services
# RESEND_API_KEY=re_...
